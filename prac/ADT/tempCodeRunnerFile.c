  // fwrite(&L, sizeof(struct studList), 1, fp);
    // rewind(fp);
    // fread(&buffer, sizeof(struct studList), 1, fp);

    // printf("FROM FILE:::");
    // display(buffer);
    // rewind(fp);

    // printf("NRUIJJJJJJJJJ");